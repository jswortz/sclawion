// htmx is loaded from a CDN in production; this stub keeps the embed FS happy
// in dev and during build. Replace with the real bundle (or load via <script src>
// from unpkg) before shipping.
//
// Pinning: htmx@1.9.x at https://unpkg.com/htmx.org@1.9.12 — copy the contents
// of dist/htmx.min.js here and re-run `go build` to pick it up. Vendoring keeps
// the admin UI usable inside VPC-SC perimeters where unpkg.com is blocked.
